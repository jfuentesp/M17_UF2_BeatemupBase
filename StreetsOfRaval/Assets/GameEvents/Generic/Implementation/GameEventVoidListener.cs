using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace streetsofraval { 
//And now we create a new GameEventListener that extends from the generic abstract class
public class GameEventVoidListener : GameEventListener { }
}